on:
  workflow_dispatch:

jobs:
  job-1:
    runs-on: ubuntu-latest
    steps:
      - run : "echo jobs ${{github.job}}"

  job-2:
    runs-on : ubuntu-latest
    needs : job-1
    steps:
     - run :  "echo jobs ${{github.job}}"

  job-3:
    runs-on : ubuntu-latest
    needs : job-1
    steps:
     - run : "echo jobs ${{github.job}}"

  job-4:
    runs-on: ubuntu-latest
    needs : [job-2,job-3]
    steps:
      - run :  "echo jobs ${{github.job}}"
    
    
  
